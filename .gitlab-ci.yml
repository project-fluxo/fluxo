# =================================================================================================================================
# Stages to be executed, each stage is a collection of jobs pointing to the stage
# =================================================================================================================================

stages:
  - build
  - short_runs

# global paths to be kept between jobs
cache:
  paths:
  - tests

# global variables
variables:
  GET_SOURCES_ATTEMPTS: 3 
  RESTORE_CACHE_ATTEMPTS: 3
  ARTIFACT_DOWNLOAD_ATTEMPTS: 3

# =================================================================================================================================
# Stage "build"
# v>9 artifacts: name: "${CI_PIPELINE_ID}_${CI_JOB_NAME}_${CI_COMMIT_REF_NAME}"
#
# SHORT SYNTAX EXPLANATIONS FOR THE JOB, FOR DETAILS VISIT:    ===> https://docs.gitlab.com/ce/ci/yaml/   <===
# "stage:"         makes the job part of a stage defined above
# "tags:"          selects the runner
# "only:"          restricts the execution of the job to a git branch or tag
# "before_script:" shell commands to be executed before the main script. 
# "script:"        shell commands for the test. If a shell command exits with >0, the job is marked as "failed", else as "passed".
#                  commands after a failing one are not executed anymore!
# "after_script:"  shell commands after passed OR failed script. Careful, the current directory is always the root of the repo!
# "artifacts:"     keep data from the job which is uploaded to gitlab.com. You really should set a duration to expiration.
#                  "when:" can be either on_failure, on_success or always
# =================================================================================================================================
build_linadv:
  stage: build 
  retry: 2
  tags:
    - vm_linux
  before_script:
    - . ~/environments/modules_for_fluxo_intel
  script: 
    - cd tests; pwd
    - if [ -n "${NIGHTLY_RUNNER}" ]; then python build_tests.py -p 2 -stage 0 -buildhdf5 0 -case 100-199 ; else python build_tests.py -p 2 -stage 0 -buildhdf5 0 -case 101,103;  fi
  after_script:
    - pwd 
  artifacts:
    name: "${CI_PIPELINE_ID}_${CI_BUILD_REF_NAME}_${CI_BUILD_NAME}"
    expire_in: 1 hour
    when: always
    paths:
    - tests/log_1*.txt

#--------------------------------------------------------------------------------------------------------------------------------
build_maxwell:
  stage: build 
  retry: 2
  tags:
    - vm_linux
  before_script:
    - . ~/environments/modules_for_fluxo_intel
  script: 
    - cd tests; pwd
    - if [ -n "${NIGHTLY_RUNNER}" ]; then python build_tests.py -p 2 -stage 0 -buildhdf5 0 -case 200-299 ; else python build_tests.py -p 2 -stage 0 -buildhdf5 0 -case 201;  fi
  after_script:
    - pwd 
  artifacts:
    name: "${CI_PIPELINE_ID}_${CI_BUILD_REF_NAME}_${CI_BUILD_NAME}"
    expire_in: 1 hour
    when: always
    paths:
    - tests/log_2*.txt

#--------------------------------------------------------------------------------------------------------------------------------
build_mhd:
  stage: build 
  retry: 2
  tags:
    - vm_linux
  before_script:
    - . ~/environments/modules_for_fluxo_intel
  script: 
    - cd tests; pwd
    - if [ -n "${NIGHTLY_RUNNER}" ]; then python build_tests.py -p 2 -stage 0 -buildhdf5 0 -case 300-399 ; else python build_tests.py -p 2 -stage 0 -buildhdf5 0 -case 301,304;  fi
  after_script:
    - pwd 
  artifacts:
    name: "${CI_PIPELINE_ID}_${CI_BUILD_REF_NAME}_${CI_BUILD_NAME}"
    expire_in: 1 hour
    when: always
    paths:
    - tests/log_3*.txt

#--------------------------------------------------------------------------------------------------------------------------------
build_nav-st:
  stage: build 
  retry: 2
  tags:
    - vm_linux
  before_script:
    - . ~/environments/modules_for_fluxo_intel
  script: 
    - cd tests; pwd
    - if [ -n "${NIGHTLY_RUNNER}" ]; then python build_tests.py -p 2 -stage 0 -buildhdf5 0 -case 400-499 ; else python build_tests.py -p 2 -stage 0 -buildhdf5 0 -case 401,403;  fi
  after_script:
    - pwd 
  artifacts:
    name: "${CI_PIPELINE_ID}_${CI_BUILD_REF_NAME}_${CI_BUILD_NAME}"
    expire_in: 1 hour
    when: always
    paths:
    - tests/log_4*.txt

# =================================================================================================================================
# Stage "short_runs"
# =================================================================================================================================
short_runs_linadv:
  stage: short_runs
  retry: 2
  tags:
    - vm_linux
  before_script:
    - . ~/environments/modules_for_fluxo_intel
  script: 
    - cd tests; pwd
    - if [ -n "${NIGHTLY_RUNNER}" ]; then python build_tests.py -p 2 -stage 2 -case 100-199 ; else python build_tests.py -p 2 -stage 2 -case 101,103;  fi
  after_script:
    - pwd 
  artifacts:
    name: "${CI_PIPELINE_ID}_${CI_BUILD_REF_NAME}_${CI_BUILD_NAME}"
    expire_in: 1 hour
    when: always
    paths:
    - tests/freestream/log_LINADV*.txt


#--------------------------------------------------------------------------------------------------------------------------------
short_runs_mhd:
  stage: short_runs
  retry: 2
  tags:
    - vm_linux
  before_script:
    - . ~/environments/modules_for_fluxo_intel
  script: 
    - cd tests; pwd
    - if [ -n "${NIGHTLY_RUNNER}" ]; then python build_tests.py -p 2 -stage 2 -case 300-399 ; else python build_tests.py -p 2 -stage 2 -case 301,304;  fi
  after_script:
    - pwd 
  artifacts:
    name: "${CI_PIPELINE_ID}_${CI_BUILD_REF_NAME}_${CI_BUILD_NAME}"
    expire_in: 1 hour
    when: always
    paths:
    - tests/freestream/log_MHD*.txt

#--------------------------------------------------------------------------------------------------------------------------------
short_runs_nav-st:
  stage: short_runs
  retry: 2
  tags:
    - vm_linux
  before_script:
    - . ~/environments/modules_for_fluxo_intel
  script: 
    - cd tests; pwd
    - if [ -n "${NIGHTLY_RUNNER}" ]; then python build_tests.py -p 2 -stage 2 -case 400-499 ; else python build_tests.py -p 2 -stage 2 -case 401,403;  fi
  after_script:
    - pwd 
  artifacts:
    name: "${CI_PIPELINE_ID}_${CI_BUILD_REF_NAME}_${CI_BUILD_NAME}"
    expire_in: 1 hour
    when: always
    paths:
    - tests/freestream/log_NAVIERSTOKES*.txt
